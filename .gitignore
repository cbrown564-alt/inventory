__pycache__/
*.pyc
.env
*.egg-info/
build/
dist/
.venv/

# model weights
*.pt
mobileclip_blt.ts

# local runs (committed example lives in examples/)
report/
capture/
/tmp-*

# benchmark run outputs: keep the scored json/html, not the copied photos
# (capture/ above already excludes the extracted benchmark photos — regenerate
# both with benchmarks/extract_inventoryflex.py + a build)
benchmarks/*/report-*/photos/

# build/run logs (stdout captures from background builds)
*.log

# own-property eval fixture (personal data; local by default per docs/07)
evals/fixtures/ownproperty/
