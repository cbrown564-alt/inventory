__pycache__/
*.pyc
.env
*.egg-info/
build/
dist/
.venv/

# model weights
*.pt
mobileclip_blt.ts

# local runs (committed example lives in examples/)
report/
capture/
/tmp-*

# benchmark run outputs: keep the scored json/html, not the copied photos
# (capture/ above already excludes the extracted benchmark photos — regenerate
# both with benchmarks/extract_inventoryflex.py + a build)
benchmarks/*/report-*/photos/
benchmarks/*/report-*/work/crops/
benchmarks/*/report-*/inventory.pdf
benchmarks/*/report-*/inventory-issue.html

# local smoke-test capture from docs/09 (regenerate with homeinventory capture)
capture-smoke/

# build/run logs (stdout captures from background builds)
*.log

# own-property eval fixture (personal data; local by default per docs/07)
evals/fixtures/ownproperty/

# own-property walkthrough source video (personal data, 1.3 GB) + Finder junk
examples/videos/IMG_5512.MOV
.DS_Store

# segmentation spike outputs (personal frames)
segment-spike*/
